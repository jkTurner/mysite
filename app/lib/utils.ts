export const capitalFirstLetter = (string: string) => {
    return string.charAt(0).toUpperCase() + string.slice(1);
}

export const capitalAll = (string: string) => {
    return string.toUpperCase();
}